#ifndef CHROMEOS_LAUNCHER_H
#define CHROMEOS_LAUNCHER_H

#include "types.h"

void toggle_chromeos_launcher(BarWindow *bw, AppState *state);
void close_all_chromeos_launchers(AppState *state);

#endif
